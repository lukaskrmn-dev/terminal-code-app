[project]
name = "terminal-code-app"
version = "0.1.0"
description = "Browser-based Shell Terminal + Code Interpreter (Go + React)"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "GPL-3.0"}

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
